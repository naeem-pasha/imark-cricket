export const metadata = {
  title: "Score Detial",
  description: "Live Score in Detail",
};

export default function RootLayout({ children }) {
  return <main className=" max-w-3xl mx-auto bg-white">{children}</main>;
}
